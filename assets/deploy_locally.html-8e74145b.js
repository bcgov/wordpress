import{_ as t,r as i,o as r,c as l,a as e,b as s,d as a,e as n}from"./app-279dd2c1.js";const c="/wordpress/images/keychain-localhost.png",d="/wordpress/images/max-upload-file-size.png",p={},h=n(`<h1 id="deploying-wordpress-to-docker" tabindex="-1"><a class="header-anchor" href="#deploying-wordpress-to-docker" aria-hidden="true">#</a> Deploying WordPress to Docker</h1><h2 id="setup" tabindex="-1"><a class="header-anchor" href="#setup" aria-hidden="true">#</a> Setup</h2><p>Clone this repository, and go to the dev directory</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># Clone WordPress deployment repository</span>
<span class="token function">git</span> clone https://github.com/bcgov/wordpress.git
<span class="token comment"># Change directory to the wordpress/dev folder</span>
<span class="token builtin class-name">cd</span> wordpress/dev
<span class="token comment"># Create an .env by copying the sample .env</span>
<span class="token function">cp</span> sample-env .env
</code></pre></div><h2 id="environment-basics" tabindex="-1"><a class="header-anchor" href="#environment-basics" aria-hidden="true">#</a> Environment Basics</h2><ul><li>Update variables inside <code>.env</code> file to reflect your setup. <ul><li>The <code>CONTENT_DIR</code> variable is the location of your WordPress content directory.</li><li>The <code>TEMP_DIR</code> variable can be used to import/export db using the wp-cli command.</li></ul></li></ul><div class="custom-container tip"><p class="custom-container-title">TIP</p><p>When Importing and Exporting databases with the <code>wp cli</code> This will be the link between the WordPress container and your local machine.</p></div><h2 id="setting-up-ssl-on-mac-required" tabindex="-1"><a class="header-anchor" href="#setting-up-ssl-on-mac-required" aria-hidden="true">#</a> Setting up SSL on Mac (required)</h2><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># sets up \`localhost\` self signed certs.</span>
<span class="token function">docker</span> compose <span class="token parameter variable">--file</span> docker-compose-init.yaml run self-signed-certificate-generator
</code></pre></div><ul><li>Drag ./Docker/nginx/ssl/localhost.crt to KeyChain app (login) section and trust always: <ul><li>Double-click the certificate.</li><li>Next to Trust, click the arrow and in the dropdown box for &quot;When using this certificate:&quot; select <code>Always Trust</code>.</li></ul></li><li>If this cert is not working, try restarting your MAC, however there might be a less drastic way of making this work.</li></ul><div class="custom-container warning"><p class="custom-container-title">WARNING</p><p>WordPress will now run on localhost port 443, which is exactly the same port that Kubernetes runs on. Therefore local Kubernetes and WordPress can&#39;t be running at the same time.</p></div><img src="`+c+`" style="max-width:250px;margin:1rem 0;"><h2 id="run-docker-compose" tabindex="-1"><a class="header-anchor" href="#run-docker-compose" aria-hidden="true">#</a> Run Docker compose</h2><div class="custom-container warning"><p class="custom-container-title">WARNING</p><p>You have to be in the wordpress/dev directory for this to work</p></div><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># Starts WordPress deployment </span>
<span class="token function">docker</span> compose up
<span class="token comment"># to re-build the images and start WordPress deployment</span>
<span class="token comment"># The build flag is required, anytime there are changes to the images.</span>
<span class="token function">docker</span> compose up <span class="token parameter variable">--build</span>
<span class="token comment"># If you enable build in shell scripts, this scripts works from anywhere</span>
<span class="token comment"># Starts WordPress deployments</span>
wp_start
</code></pre></div>`,15),u={href:"https://localhost",target:"_blank",rel:"noopener noreferrer"},m=e("li",null,[s("Access PHPMyAdmin at "),e("a",{href:"http:/localhost:8081"},"http://localhost:8081"),s(" (Note: does not use https)")],-1),g=n('<h2 id="optional-multi-site-recommended" tabindex="-1"><a class="header-anchor" href="#optional-multi-site-recommended" aria-hidden="true">#</a> Optional multi-site (Recommended):</h2><ul><li><p>In the WordPress admin UI, go to menu Tools -&gt; Network Setup and click install</p></li><li><p>Bring down the WordPress site <code>docker compose down</code> (or &lt;CTRL&gt;-C if docker is in the foreground and then <code>docker compose down</code>).</p></li><li><p>In your <code>.env</code> file, ensure</p><ul><li><code>MULTISITE=1</code></li></ul></li><li><p>Bring up WordPress site <code>docker compose up</code></p></li><li><p>In the WordPress admin UI, go to My Sites -&gt; Network Admin -&gt; Settings and <code>change Max upload file size</code> to 10000 and click <code>Save Changes</code></p></li></ul><img src="'+d+`" style="max-width:250px;margin:1rem 0;"><h2 id="running-thereafter" tabindex="-1"><a class="header-anchor" href="#running-thereafter" aria-hidden="true">#</a> Running Thereafter:</h2><ul><li><p><code>docker compose up</code> runs the nginx, db, WordPress php-fpm, and wp-cli containers, this will also output debug.log in the stdout.</p></li><li><p>WordPress wp-content directory, can be found at the path specified in your <code>.env</code> under variable <code>CONTENT_DIR</code>, .</p></li></ul><h2 id="helper-functions" tabindex="-1"><a class="header-anchor" href="#helper-functions" aria-hidden="true">#</a> Helper functions.</h2><p>The <a href="./bin/commands.sh">Helper functions</a> can be linked to run anytime a new terminal window is open by adding it to your <code>~/.bash_profile</code></p><h3 id="including-helper-functions-in-bash-profile" tabindex="-1"><a class="header-anchor" href="#including-helper-functions-in-bash-profile" aria-hidden="true">#</a> Including helper functions in bash profile.</h3><div class="custom-container tip"><p class="custom-container-title">TIP</p><p>In order to make the helper functions accessible you need to include them into your bash or zsh profile. These commands then can be run from anywhere on your file system from a terminal window.</p></div><p>If you are using bash add this to your <code>~/.bash_profile</code></p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token keyword">if</span> <span class="token punctuation">[</span> <span class="token parameter variable">-f</span> /location-of-this-repo/wordpress/dev/bin/commands.sh <span class="token punctuation">]</span> <span class="token punctuation">;</span> <span class="token keyword">then</span>
 <span class="token builtin class-name">.</span> /location-of-this-repo/wordpress/dev/bin/commands.sh
<span class="token keyword">fi</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Once this is done you can use the following commands from any directory</p>`,12),b=n("<li><code>wp_start</code> - Starts WordPress local deployment (docker)</li><li><code>wp_stop</code> - Stops WordPress local deployment (docker)</li><li><code>wp_log</code> - Tails the debug.log in the content directory</li><li><code>gowp</code> - Goes to plugin directory</li><li><code>gowp themes</code> - Goes to themes directory</li><li><code>wpgrep</code> - Does a grep with certain excludes to directories like .git, node_modules, vendor</li>",6),f=e("code",null,"wp",-1),w={href:"https://wp-cli.org/",target:"_blank",rel:"noopener noreferrer"},k=n(`<h2 id="example-wordpress-command-line-using-wp" tabindex="-1"><a class="header-anchor" href="#example-wordpress-command-line-using-wp" aria-hidden="true">#</a> Example WordPress Command Line using wp.</h2><ul><li><code>wp site list</code> - Shows all the sites.</li><li><code>wp plugin list</code> - gets all the plugins.</li><li><code>wp plugin update akismet</code> - updates akismet plugin</li></ul><h2 id="backing-up-full-database" tabindex="-1"><a class="header-anchor" href="#backing-up-full-database" aria-hidden="true">#</a> Backing up full database</h2><p>This file will get stored in a directory that you determined in your <code>.env</code> as the <code>TEMP_DIR</code> variable.</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># In order for this command to work you need the following in your terminal profile (bash or zsh)</span>
<span class="token comment"># . /location-of-this-repo/wordpress/dev/bin/commands.sh as shown above</span>
wp db <span class="token builtin class-name">export</span> /tmp/WordPress/all-sites.sql --add-drop-table
</code></pre></div><h2 id="restoring-full-database" tabindex="-1"><a class="header-anchor" href="#restoring-full-database" aria-hidden="true">#</a> Restoring full database</h2><div class="custom-container warning"><p class="custom-container-title">WARNING</p><p>This command will replace your entire database, before running <code>wp db import</code> make sure you understand what this command does.</p></div><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>wp db <span class="token function">import</span> /tmp/WordPress/all-sites.sql
</code></pre></div>`,8);function y(v,_){const o=i("ExternalLinkIcon");return r(),l("div",null,[h,e("ul",null,[e("li",null,[s("Access Wordpress at "),e("a",u,[s("https://localhost"),a(o)])]),m]),g,e("ul",null,[b,e("li",null,[f,s(" - "),e("a",w,[s("Wordpress Command line"),a(o)]),s(" that allows an endless amount of things to be completed with your local WordPress instance.")])]),k])}const W=t(p,[["render",y],["__file","deploy_locally.html.vue"]]);export{W as default};
